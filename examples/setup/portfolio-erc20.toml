# Token-only portfolio snapshot example. Import it before starting the sole public objective.
# The live EVM route remains process-local runtime configuration; no endpoint or token decimal
# authority is stored here.

[[values]]
name = "acme/erc20-mainnet"
kind = "portfolio"

[values.value]
portfolio_id = "portfolio_erc20_mainnet"
quote_codes = ["USD"]
metadata = {}

[[values.value.networks]]
family = "evm"
network_id = "ethereum-mainnet"
chain_id = 1
native_decimals = 18
metadata = {}

[[values.value.wallets]]
wallet_id = "wallet_usdc_mainnet"
network_id = "ethereum-mainnet"
symbol_ids = ["usdc.ethereum-mainnet"]
metadata = {}

[values.value.wallets.subject]
kind = "evm_address"
address = "0x000000000000000000000000000000000000dead"

[values.value.wallets.implementation]
kind = "address_only"

[[values.value.symbol_configs]]
symbol_id = "usdc.ethereum-mainnet"
display_symbol = "USDC"
network_id = "ethereum-mainnet"
metadata = {}

[values.value.symbol_configs.source]
kind = "erc20"
contract_address = "0xa0b86991c6218b36c1d19d4a2e9eb0ce3606eb48"

[values.value.symbol_configs.valuation]
quotes = [
  { quote = "USD", priced_symbol_id = "usdc.ethereum-mainnet", unit_price_dec = "1.00" },
]
