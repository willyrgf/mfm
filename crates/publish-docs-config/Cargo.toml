[package]
name = "mfm-publish-docs-config"
version = "0.1.0"
edition = "2021"
description = "Shared authored and canonical desired-catalog config pipeline for publish-docs"
repository = "https://github.com/willyrgf/mfm"
license = "MIT"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
toml = "0.8"

mfm-authored-config = { path = "../authored-config", version = "0.1.0" }
