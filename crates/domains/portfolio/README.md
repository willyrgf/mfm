# mfm-portfolio

Portfolio owns selector validation, trusted configuration/binding resolution, one singular snapshot
admission context, final Program authoring, and declaration-ordered cumulative consolidation. Each
child EVM collection receives and returns the exact opaque Portfolio continuation; Portfolio alone
interprets it, appends the collection result, maps EVM failure metadata, and projects the frozen
snapshot/report result.
