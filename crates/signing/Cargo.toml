[package]
name = "mfm-signing"
version = "0.1.0"
edition = "2021"
description = "Public signing metadata and bounded signer contracts for MFM"
repository = "https://github.com/willyrgf/mfm"
license = "MIT"

[package.metadata.mfm]
layer = "signing"

[dependencies]
mfm-ids = { path = "../kernel/ids" }
serde = { workspace = true }
thiserror = { workspace = true }
