# mfm-capabilities

Capability contracts own canonical intent, closed evidence, mode, fact-selection request, and
bounded entry discipline. A State success carries a validated coordinate-free
`FactProposalSet`; failures carry no facts. Adapter ingress creates call-bound evidence; retained
history uses a separate qualified evidence product.
