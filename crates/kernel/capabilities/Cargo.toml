[package]
name = "mfm-capabilities"
version = "0.1.0"
edition = "2021"
description = "Capability contracts for the MFM typed kernel"
repository = "https://github.com/willyrgf/mfm"
license = "MIT"
readme = "README.md"

[lib]
name = "mfm_capabilities"
path = "src/lib.rs"

[dependencies]
mfm-effects = { path = "../effects" }
mfm-ids = { path = "../ids" }

[dev-dependencies]
trybuild = "1.0"
