# mfm-runtime

Runtime owns immutable live assembly, affine sessions, direct-new `CommittedCall`, typed handoff,
and `PendingConclusion` around Store. Provider entry requires the consuming call token.
