# mfm-store

Store validates complete frames, reduces one sequential prefix, selects one preparation, enforces
occurrence-level conclusion uniqueness, and returns exact-head append owners. It cannot invoke
Runtime or provider code.
