# mfm-facts

Typed kernel crate for fact descriptors, fact identity, field extraction contracts, visibility, and
query evidence.

`docs/RFC_COLLECTORS.md` defines the target facts and collectors contract. `docs/design.md` remains
the normative typed-core authority contract. This crate is framework-owned and must remain
domain-free.
