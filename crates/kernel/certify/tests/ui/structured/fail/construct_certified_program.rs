#![allow(unreachable_code)]

use mfm_certify::structured::CertifiedProgram;

fn construct() -> CertifiedProgram {
    CertifiedProgram {
        authored: panic!(),
        expanded: panic!(),
        profile: panic!(),
        proof: panic!(),
        coverage: panic!(),
        document: panic!(),
    }
}

fn main() {}
