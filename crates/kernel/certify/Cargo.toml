[package]
name = "mfm-certify"
version = "0.1.0"
edition = "2021"
description = "Certification contracts for MFM typed execution specs"
repository = "https://github.com/willyrgf/mfm"
license = "MIT"
readme = "README.md"

[package.metadata.mfm]
layer = "kernel"
domain-facing = true
binary-facing = false

[lib]
name = "mfm_certify"
path = "src/lib.rs"

[dependencies]
thiserror = { workspace = true }
mfm-capabilities = { path = "../capabilities" }
mfm-canonical = { path = "../canonical" }
mfm-effects = { path = "../effects" }
mfm-events = { path = "../events" }
mfm-ids = { path = "../ids" }
mfm-program = { path = "../program" }
mfm-spec = { path = "../spec" }
mfm-values = { path = "../values" }
serde_json = { workspace = true }

[dev-dependencies]
mfm-program-derive = { path = "../program-derive" }
serde = { workspace = true }
trybuild = "1.0"
