[package]
name = "mfm-program"
version = "0.1.0"
edition = "2021"
description = "Typed state-program authoring contracts for MFM"
repository = "https://github.com/willyrgf/mfm"
license = "MIT"
readme = "README.md"

[package.metadata.mfm]
category = "kernel"

[lib]
name = "mfm_program"
path = "src/lib.rs"

[dependencies]
mfm-capabilities = { path = "../capabilities" }
mfm-canonical = { path = "../canonical" }
mfm-effects = { path = "../effects" }
mfm-ids = { path = "../ids" }
mfm-values = { path = "../values" }
serde_json = { workspace = true }

[dev-dependencies]
mfm-program-derive = { path = "../program-derive" }
serde = { workspace = true }
trybuild = "1.0"
