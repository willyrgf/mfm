[package]
name = "mfm-spec"
version = "0.1.0"
edition = "2021"
description = "Certified typed execution spec contracts for MFM"
repository = "https://github.com/willyrgf/mfm"
license = "MIT"
readme = "README.md"

[package.metadata.mfm]
layer = "kernel"
domain-facing = false
binary-facing = false

[lib]
name = "mfm_spec"
path = "src/lib.rs"

[dependencies]
mfm-canonical = { path = "../canonical" }
mfm-facts = { path = "../facts" }
mfm-ids = { path = "../ids" }
mfm-program-derive = { path = "../program-derive" }
mfm-values = { path = "../values" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
