#![warn(missing_docs)]
//! Derive macros for MFM typed program contracts.
//!
//! This proc-macro crate is intentionally skeletal until the derive contract is implemented. It
//! must remain independent from runtime, storage, old dynamic machine, SDK, and domain crates.
