# mfm-state-keystore

Reusable keystore administration and transaction states for MFM workflows.

This crate keeps keystore execution logic in shared `State` implementations so keystore-related ops can remain thin planners.
