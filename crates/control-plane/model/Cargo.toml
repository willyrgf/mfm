[package]
name = "mfm-control-plane-model"
version = "0.1.0"
edition = "2021"
description = "Backend-neutral control-plane stream-family model and projections"
repository = "https://github.com/willyrgf/mfm"
license = "MIT"

[dependencies]
mfm-machine = { path = "../../machine", version = "0.1.0" }
serde = { workspace = true }
serde_json = { workspace = true }
