[package]
name = "mfm-event-store-postgres"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = { workspace = true }
mfm-machine = { path = "../../machine" }
serde_json = { workspace = true }
tokio = { workspace = true }
tokio-postgres = { version = "0.7.13", features = ["with-serde_json-1", "with-uuid-1"] }
tracing = { workspace = true }

[dev-dependencies]
mfm-machine-test-support = { path = "../../machine-test-support" }
tokio = { workspace = true }

[features]
parity-tests = []
