# mfm-sdk

Thin orchestration helpers over `mfm-machine` (planning, launching, resuming).

Unstable helpers additionally include a single-op execution/report extraction endpoint used by thin CLI wrappers (`mfm_sdk::unstable::execute_single_op_report`).
