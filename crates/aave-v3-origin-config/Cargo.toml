[package]
name = "mfm-aave-v3-origin-config"
version = "0.1.0"
edition = "2021"
description = "Shared authored, canonical, and built config pipeline for Aave V3 Origin stack workflows"
repository = "https://github.com/willyrgf/mfm"
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
toml = "0.8"

mfm-authored-config = { path = "../authored-config", version = "0.1.0" }
mfm-evm-core = { path = "../evm-core", version = "0.1.0" }
mfm-machine = { path = "../machine", version = "0.1.0" }
