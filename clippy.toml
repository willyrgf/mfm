disallowed-methods = [
    { path = "mfm_machine::io::IoProvider::call", reason = "Use typed IO clients from collector or bridge modules instead of raw IoProvider::call." },
    { path = "std::env::var", reason = "Resolve environment defaults in CLI/app or transport assembly layers instead of op/state crates." },
    { path = "std::env::var_os", reason = "Resolve environment defaults in CLI/app or transport assembly layers instead of op/state crates." },
]
