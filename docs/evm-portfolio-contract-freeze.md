# EVM Read and Portfolio contract freeze

Status: current native boundary and product projection contract.

## Material uncertainties

none within this observational product boundary. Production transaction finality remains outside
Portfolio; managed lifecycle acceptance uses the pinned non-reorging Reth contract.

[portfolio-snapshot.json](contracts/evm-portfolio/portfolio-snapshot.json) freezes the native client's
public success projection and is checked by Application acceptance. It is not the semantic
Portfolio output or the Program/Journal wire. The former native collection and root-mapped failure
fixtures are removed with their superseded contracts; current shared handoff and exact native/product
failure coverage is mapped in [dsl-phase-b.md](dsl-phase-b.md).

Native clients admit EVM configuration and convert semantic success and exact retained failures.
Portfolio owns collection/context agreement, checked arithmetic and product projections. Chain
owns semantic requests, observations and cumulative contexts. Native implementations own EVM
planning, route/ledger qualification, ABI decoding and State-derived failure decoder dispatch.
Application coordinates construction, execution, inspection and publication without interpreting EVM.

Independent expected route references survive construction and cold loading. Local route mismatch
is Internal with no provider call or append. Only authenticated external evidence can produce
IntegrityBlocked. Rejected and SafeFailure retain their existing unsuccessful meanings and exact
native originals; public failure projection never replaces the canonical report.

Public route descriptors retained with semantic demand allow configuration-deleted publication
without a live binding inventory. Exact Program and changed schema identities reject old wires;
there is no compatibility decoder or history rewrite.

Portfolio exposes no transaction entry point. Maintained shared lifecycle Operations and native
EVM recipes own deployment/configuration/observation; managed acceptance proves 42/84, cold retained
commands and delayed settlement. That lifecycle does not add a mutation surface to Portfolio.
