# Scope note

Status: superseded one-time pre-cutover artifact. Retained only through the first cutover commit;
it is not current product authority.

Bitcoin provider routing is outside the two-entry-point MVP cutover. A future Bitcoin operation
must define its own singular `C0`, strict capability evidence, immutable binding, and sequential
Program before it is added to the product graph.
